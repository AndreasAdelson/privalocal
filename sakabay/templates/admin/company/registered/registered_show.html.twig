{% extends 'base.html.twig' %}

{% block title %}{{ 'company_registered_title_show_page'|trans }}{% endblock %}

{% block body %}
    <company-registered-show
    :company-id="{{ companyId }}"
    :can-edit="{{ canEdit|json_encode }}"
    url-precedente="{{ urlPrecedente }}"
    token="{{ csrf_token('companySubscription') }}"
    ></company-registered-show>
{% endblock %}
