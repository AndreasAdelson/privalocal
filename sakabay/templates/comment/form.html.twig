{% extends 'base.html.twig' %}

{% block title %}
{{ 'comment_title_create_page'|trans }}
{% endblock %}

{% block body %}
 <form-comment
  :utilisateur-id="{{ utilisateurId }}"
  url-precedente="{{ urlPrecedente }}"
  :company-id="{{ companyId }}"
  token="{{ csrf_token('comment') }}"
  >
 </form-comment>
{% endblock %}
